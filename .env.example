# Copy only — never commit real secrets.

# Shared
NODE_ENV=development
DATABASE_URL=postgresql://rpm:rpm@localhost:5432/rpm?schema=public

# API
API_HOST=0.0.0.0
API_PORT=3001

# Worker
# (uses DATABASE_URL when consumers are added)

# Web (Next.js)
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001

# Future auth cookies / Redis / S3 (names only)
# REDIS_URL=redis://localhost:6379
# S3_ENDPOINT=
# S3_BUCKET=
# S3_ACCESS_KEY_ID=
# S3_SECRET_ACCESS_KEY=
# AUTH_COOKIE_DOMAIN=localhost
